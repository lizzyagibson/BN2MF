---
title: "GNMF & Sim Data -- Iterate"
author: "Lizzy Gibson"
date: "2/25/2020"
output: 
  html_document:
    toc: TRUE
    toc_float: TRUE
---

```{r setup, include=FALSE}
options(scipen = 999)
library(MNdata) # my data
library(reshape2) # melt
library(gridExtra) # gridarrange
library(bindata) # multivariate binomial dist
library(NMF) # rmatrix
library(MASS) # mvrnorm
library(tidyverse)
library(R.matlab)
```

# Load Sims

```{r}
load(here::here("./Sims/Iterate/sim_dist.RDA"))
sim_dist

load(here::here("./Sims/Iterate/sim_cor.RDA"))
sim_cor

load(here::here("./Sims/Iterate/sim_over.RDA"))
sim_over

labels <-  c("pcb28", "pcb66", "pcb74", "pcb99", "pcb105", "pcb118", "pcb138_158", "pcb146", "pcb153", "pcb156", "pcb167",
          "pcb170", "pcb178", "pcb183", "pcb187", "pcb180", "pcb189", "pcb194", "pcb196_203", "pcb199", "pcb206", "pcb209",
          "BDE17", "BDE28", "BDE47", "BDE66", "BDE85", "BDE99", "BDE100", "BDE153", "BDE154", "BDE183", "BDE209", "MECPP",
          "MEHHP", "MEOHP", "MCPP", "MIBP", "MBP", "MBZP", "MEP", "MEHP", "dcp_24", "dcp_25", "b_pb", "bp_3", "m_pb",
          "p_pb", "tcs", "bpa")
```

# GNMF Results

## 1. Distinct Patterns

```{r}
get_eh_dist <- function(seed) {
  eh <- readMat(here::here(paste0("./Sims/Iterate_Out/eh_dist_", seed, ".mat")))[[1]]
  eh <- as_tibble(eh)
}

eh_dist <- tibble(seed = 1:100) %>% 
  mutate(eh = map(seed, ~get_eh_dist(.x)))

for (i in 1:100) {
  colnames(eh_dist$eh[[i]]) <- labels
  
  eh_dist$eh[[i]] <- eh_dist$eh[[i]]
  }
```

```{r}
eh_dist_unnest <- eh_dist %>% 
  unnest(cols = c(eh))

# Reorder patterns
eh_dist_unnest %>% 
  mutate(pattern1 = ifelse(pcb146 > 0.5 & pcb28 > bpa, 1,
                          ifelse(BDE17 > 0.5, 2,
                                ifelse(MECPP > 0.5, 3,
                                       ifelse(tcs > 0.5, 4, 5))))) %>% pull(pattern1) %>% table()
```

### Scores

```{r}
get_ewa_dist <- function(seed) {
  ewa <- readMat(here::here(paste0("./Sims/Iterate_Out/ewa_dist_", seed, ".mat")))[[1]]
  ewa <- as_tibble(ewa)
}

ewa_dist <- tibble(seed = 1:100) %>% 
  mutate(ewa = map(seed, ~get_ewa_dist(.x)))

ewa_dist_unnest <- ewa_dist %>% 
  unnest(cols = c(ewa))
```

#### Match Scores

```{r}
full_dist <- full_join(eh_dist, ewa_dist, by = "seed") %>% 
    full_join(., sim_dist, by = "seed") %>% 
  select(-chem)

match_patterns <- function (eh_unnested) {
  eh_reordered <- eh_unnested %>% 
  mutate(pattern1 = ifelse(pcb146 > 0.5 & pcb28 > bpa, 1,
                          ifelse(BDE17 > 0.5, 2,
                                ifelse(MECPP > 0.5, 3,
                                       ifelse(tcs > 0.5, 4, 5))))) %>% # Switch patterns later to compare EH to loadings
  select(pattern1, everything())
  eh_reordered
}

match_scores <- function (patterns, scores) {
  N <- nrow(scores)
  for (j in 1:nrow(patterns)) {
    for (i in 1:ncol(scores)) {
      if (patterns[j, 2] == j && scores[1, i] == j) {
        scores[(N+1),i] = patterns[j,1]
      }
    }
  } 
  scores <- scores[c(1, nrow(scores), 2:(nrow(scores) - 1)),]
  scores
}

scores_matched_dist <- full_dist %>% 
    mutate(eh = map(eh, ~cbind(pattern = 1:nrow(.x), .x)),
         ewa = map(ewa, ~rbind(pattern = 1:ncol(.x), .x)),
         eh_reordered = map(eh, ~match_patterns(eh_unnested = .x)),
         ewa_reordered = ewa,
         ewa_reordered = map2(eh_reordered, ewa_reordered, ~match_scores(patterns = .x, scores = .y)))
```

#### Arrange Scores

```{r}
reorder_patterns <- function (eh_unnested) {
  eh_reordered <- eh_unnested %>% 
  arrange(pattern1) %>%
  mutate(pattern1 = fct_inorder(as.factor(pattern1)),
         pattern1 = as.integer(pattern1)) %>% 
    select(-pattern1, -pattern)
  eh_reordered
}

reorder_scores <- function (ewa_unnested) {
  ewa_reordered <- ewa_unnested %>% 
    t(.) %>% as_tibble(.) %>% 
  arrange(V2) %>%
  mutate(V2 = fct_inorder(as.factor(V2)),
         V2 = as.integer(V2)) %>% 
    t(.) %>% as_tibble(.) %>% 
    slice(3:nrow(.))
  ewa_reordered
}

scores_ordered_dist <- scores_matched_dist %>% 
  mutate(eh_reordered = map(eh_reordered, ~reorder_patterns(.x)),
         ewa_reordered = map(ewa_reordered, ~reorder_scores(.x)))
```

### Norms

```{r}
# All
full_dist %>%
  filter(!(seed %in% c(19, 21, 39))) %>% 
  mutate(ewa = map(ewa, as.matrix),
         eh = map(eh, as.matrix)) %>% 
  mutate(pred = map2(ewa, eh, `%*%`)) %>% 
  mutate(norm_all = map2(sim, pred, ~norm(as.matrix(.x) - as.matrix(.y), "F")/norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_all) %>% 
  pull(norm_all) %>% summary()
  
# Loadings (ordered, not standardized)
scores_ordered_dist %>% 
  filter(!(seed %in% c(19, 21, 39))) %>% 
  mutate(norm_loadings = map2(patterns, eh_reordered, 
                              ~norm(as.matrix(.x) - as.matrix(.y), "F")/norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_loadings) %>% 
  pull(norm_loadings) %>% summary()
```

```{r}
# Scores (ordered, not standardized)
scores_ordered_dist %>% 
  filter(!(seed %in% c(19, 21, 39))) %>% 
  mutate(norm_scores = map2(scores, ewa_reordered, 
                              ~norm(as.matrix(.x) - as.matrix(.y), "F")/norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_scores) %>% 
  pull(norm_scores) %>% summary()
```

### Viz

```{r}
eh_dist_reorder <- eh_dist_unnest %>%
  mutate(pattern1 = ifelse(pcb146 > 0.5 & pcb28 > bpa, 1,
                          ifelse(BDE17 > 0.5, 2,
                                ifelse(MECPP > 0.5, 3,
                                       ifelse(tcs > 0.5, 4, 5))))) %>%
  select(seed, pattern1, everything()) %>%
  arrange(seed, pattern1) %>%
  mutate(pattern1 = fct_inorder(as.factor(pattern1)),
         pattern1 = as.integer(pattern1))

eh_dist_reorder %>%
  gather(key = Chemicals, value = Loadings, -pattern1, -seed) %>%
  mutate(Chemicals = fct_inorder(Chemicals)) %>% 
  mutate(Group = ifelse(str_detect(Chemicals, "BDE"), "PBDEs",
                                                     ifelse(str_detect(Chemicals, "^M"), "Phthalates",
                                                            ifelse(str_detect(Chemicals, "pcb"), "PCBs",
                                                                   "Phenols")))) %>% 
  ggplot(aes(x = Chemicals, y = Loadings)) + geom_col(aes(fill = Group)) +
  facet_wrap(.~pattern1) + theme_bw() +
  theme(axis.text.x = element_blank(),
        legend.position = "bottom",
        strip.background = element_blank())

eh_dist_reorder %>% 
  gather(key = Chemicals, value = Loadings, -pattern1, -seed) %>% 
  mutate(Chemicals = fct_inorder(Chemicals)) %>% 
  mutate(Group = ifelse(str_detect(Chemicals, "BDE"), "PBDEs",
                                                     ifelse(str_detect(Chemicals, "^M"), "Phthalates",
                                                            ifelse(str_detect(Chemicals, "pcb"), "PCBs",
                                                                   "Phenols")))) %>% 
  ggplot(aes(x = Group, y = Loadings)) + geom_jitter(aes(color = Group), alpha = 0.5, size = 0.1, height = 0) +
  geom_boxplot(aes(x = Group, color = Group), width = .45, outlier.shape = NA) +
  facet_wrap(.~pattern1) + theme_bw() +
  theme(legend.position = "none",
        strip.background = element_blank())
```

## 2. Correlated Scores

```{r}
get_eh_cor <- function(seed) {
  eh <- readMat(here::here(paste0("./Sims/Iterate_Out/eh_cor_", seed, ".mat")))[[1]]
  eh <- as_tibble(eh)
}

eh_cor <- tibble(seed = 1:100) %>% 
  mutate(eh = map(seed, ~get_eh_cor(.x)))

for (i in 1:100) {
  colnames(eh_cor$eh[[i]]) <- labels
  
  eh_cor$eh[[i]] <- eh_cor$eh[[i]]
  }

eh_cor_unnest <- eh_cor %>% 
  unnest(cols = c(eh)) 
```

```{r}
# Reorder patterns
eh_cor_unnest %>% 
  mutate(pattern1 = ifelse(pcb146 > 0.5 & pcb28 > bpa, 1,
                          ifelse(BDE17 > 0.5, 2,
                                ifelse(MECPP > 0.5, 3,
                                       ifelse(tcs > 0.5, 4, 5))))) %>% pull(pattern1) %>% table()
```

#### Scores

```{r}
get_ewa_cor <- function(seed) {
  ewa <- readMat(here::here(paste0("./Sims/Iterate_Out/ewa_cor_", seed, ".mat")))[[1]]
  ewa <- as_tibble(ewa)
}

ewa_cor <- tibble(seed = 1:100) %>% 
  mutate(ewa = map(seed, ~get_ewa_cor(.x)))

ewa_cor_unnest <- ewa_cor %>% 
  unnest(cols = c(ewa))
```

#### Match Scores

```{r}
full_cor <- full_join(eh_cor, ewa_cor, by = "seed") %>% 
    full_join(., sim_cor, by = "seed") %>% 
  select(-chem)

scores_matched_cor <- full_cor %>% 
  mutate(eh = map(eh, ~cbind(pattern = 1:nrow(.x), .x)),
         ewa = map(ewa, ~rbind(pattern = 1:ncol(.x), .x)),
         eh_reordered = map(eh, ~match_patterns(eh_unnested = .x)),
         ewa_reordered = ewa,
         ewa_reordered = map2(eh_reordered, ewa_reordered, ~match_scores(patterns = .x, scores = .y)))
```

#### Arrange Scores

```{r}
scores_ordered_cor <- scores_matched_cor %>% 
  mutate(eh_reordered = map(eh_reordered, ~reorder_patterns(.x)),
         ewa_reordered = map(ewa_reordered, ~reorder_scores(.x)))
```

### Norms

```{r}
# All
full_cor %>%
  mutate(ewa = map(ewa, as.matrix),
         eh = map(eh, as.matrix)) %>% 
  mutate(pred = map2(ewa, eh, `%*%`)) %>% 
  mutate(norm_all = map2(sim, pred, ~norm(as.matrix(.x) - as.matrix(.y), "F")/norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_all) %>% 
  pull(norm_all) %>% summary()

# Loadings (ordered, not standardized)
scores_ordered_cor %>% 
  mutate(norm_loadings = map2(patterns, eh_reordered, 
                              ~norm(as.matrix(.x) - as.matrix(.y), "F")/norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_loadings) %>% 
  pull(norm_loadings) %>% summary()
```

```{r}
# Scores (ordered, not standardized)
scores_ordered_cor %>% 
  mutate(norm_scores = map2(scores, ewa_reordered, 
                              ~norm(as.matrix(.x) - as.matrix(.y), "F")/norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_scores) %>% 
  pull(norm_scores) %>% summary()
```

### Viz

```{r}
eh_cor_reorder <- eh_cor_unnest %>%
  mutate(pattern1 = ifelse(pcb146 > 0.5 & pcb28 > bpa, 1,
                          ifelse(BDE17 > 0.5, 2,
                                ifelse(MECPP > 0.5, 3,
                                       ifelse(tcs > 0.5, 4, 5))))) %>%
  select(seed, pattern1, everything()) %>%
  arrange(seed, pattern1) %>%
  mutate(pattern1 = fct_inorder(as.factor(pattern1)),
         pattern1 = as.integer(pattern1))

eh_cor_unnest %>%  
  mutate(pattern1 = ifelse(pcb146 > 0.5 & pcb28 > bpa, 1,
                          ifelse(BDE17 > 0.5, 2,
                                ifelse(MECPP > 0.5, 3,
                                       ifelse(tcs > 0.5, 4, 5))))) %>% 
  filter(seed == 3) %>% 
  gather(key = Chemicals, value = Loadings, -pattern1, -seed) %>%
  mutate(Chemicals = fct_inorder(Chemicals)) %>% 
  mutate(Group = ifelse(str_detect(Chemicals, "BDE"), "PBDEs",
                                                     ifelse(str_detect(Chemicals, "^M"), "Phthalates",
                                                            ifelse(str_detect(Chemicals, "pcb"), "PCBs",
                                                                   "Phenols")))) %>% 
  ggplot(aes(x = Chemicals, y = Loadings)) + geom_col(aes(fill = Group)) +
  facet_wrap(.~pattern1) + theme_bw() +
  theme(axis.text.x = element_blank(),
        legend.position = "bottom",
        strip.background = element_blank())

eh_cor_unnest %>%  
  mutate(pattern1 = ifelse(pcb146 > 0.5, 1,
                          ifelse(BDE17 > 0.5, 2,
                                ifelse(MECPP > 0.5, 3,
                                       ifelse(tcs > 0.5, 4, 5))))) %>% 
  gather(key = Chemicals, value = Loadings, -pattern1, -seed) %>%
  mutate(Chemicals = fct_inorder(Chemicals)) %>% 
  mutate(Group = ifelse(str_detect(Chemicals, "BDE"), "PBDEs",
                                                     ifelse(str_detect(Chemicals, "^M"), "Phthalates",
                                                            ifelse(str_detect(Chemicals, "pcb"), "PCBs",
                                                                   "Phenols")))) %>% 
  ggplot(aes(x = Group, y = Loadings)) + geom_jitter(aes(color = Group), alpha = 0.5, size = 0.1, height = 0) +
  geom_boxplot(aes(x = Group, color = Group), width = .4, outlier.shape = NA) +
  facet_wrap(.~pattern1) + theme_bw() +
  theme(legend.position = "none",
        strip.background = element_blank())
```

## 3. Overlapping Patterns

```{r}
get_eh_over <- function(seed) {
  eh <- readMat(here::here(paste0("./Sims/Iterate_Out/eh_over_", seed, ".mat")))[[1]]
  eh <- as_tibble(eh)
}

eh_over <- tibble(seed = 1:100) %>% 
  mutate(eh = map(seed, ~get_eh_over(.x)))

for (i in 1:100) {
  colnames(eh_over$eh[[i]]) <- labels
  
  eh_over$eh[[i]] <- eh_over$eh[[i]]
  }

eh_over_unnest <- eh_over %>% 
  unnest(cols = c(eh))

eh_over_unnest %>% 
  mutate(pattern1 = ifelse(pcb146 > 0.5 & pcb28 > bpa, 1,
                          ifelse(BDE17 > 0.5, 2,
                                ifelse(MECPP > 0.5, 3,
                                       ifelse(tcs > 0.5, 4, 5))))) %>% pull(pattern1) %>% table()
```

### Scores

```{r}
get_ewa_over <- function(seed) {
  ewa <- readMat(here::here(paste0("./Sims/Iterate_Out/ewa_over_", seed, ".mat")))[[1]]
  ewa <- as_tibble(ewa)
}

ewa_over <- tibble(seed = 1:100) %>% 
  mutate(ewa = map(seed, ~get_ewa_over(.x)))

ewa_over_unnest <- ewa_over %>% 
  unnest(cols = c(ewa))

ewa_over_unnest %>% drop_na() %>% distinct(seed)
```

#### Match Scores

```{r}
full_over <- full_join(eh_over, ewa_over, by = "seed") %>% 
    full_join(., sim_over, by = "seed") %>% 
  select(-chem)

scores_matched_over <- full_over %>% 
  mutate(eh = map(eh, ~cbind(pattern = 1:nrow(.x), .x)),
         ewa = map(ewa, ~rbind(pattern = 1:ncol(.x), .x)),
         eh_reordered = map(eh, ~match_patterns(eh_unnested = .x)),
         ewa_reordered = ewa,
         ewa_reordered = map2(eh_reordered, ewa_reordered, ~match_scores(patterns = .x, scores = .y)))
```

#### Arrange Scores

```{r}
scores_ordered_over <- scores_matched_over %>% 
  mutate(eh_reordered = map(eh_reordered, ~reorder_patterns(.x)),
         ewa_reordered = map(ewa_reordered, ~reorder_scores(.x)))
```

### Norms

```{r}
# All
full_cor %>%
  mutate(ewa = map(ewa, as.matrix),
         eh = map(eh, as.matrix)) %>% 
  mutate(pred = map2(ewa, eh, `%*%`)) %>% 
  mutate(norm_all = map2(sim, pred, ~norm(as.matrix(.x) - as.matrix(.y), "F")/norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_all) %>% 
  pull(norm_all) %>% summary()

# Loadings (ordered, not standardized)
scores_ordered_over %>% 
  filter(!(seed %in% c(26, 64))) %>% 
  mutate(norm_loadings = map2(patterns, eh_reordered, 
                              ~norm(as.matrix(.x) - as.matrix(.y), "F")/norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_loadings) %>% 
  pull(norm_loadings) %>% summary()
```

```{r}
# Scores (ordered, not standardized)
scores_ordered_over %>% 
  filter(!(seed %in% c(26, 64))) %>% 
  mutate(norm_scores = map2(scores, ewa_reordered, 
                              ~norm(as.matrix(.x) - as.matrix(.y), "F")/norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_scores) %>% 
  pull(norm_scores) %>% summary()
```

### Viz

```{r}
eh_over_reorder <- eh_over_unnest %>%
  mutate(pattern1 = ifelse(pcb146 > 0.5 & pcb28 > bpa, 1,
                          ifelse(BDE17 > 0.5, 2,
                                ifelse(MECPP > 0.5, 3,
                                       ifelse(tcs > 0.5, 4, 5))))) %>%
  select(seed, pattern1, everything()) %>%
  arrange(seed, pattern1) %>%
  mutate(pattern1 = fct_inorder(as.factor(pattern1)),
         pattern1 = as.integer(pattern1))

eh_over_reorder %>% 
  gather(key = Chemicals, value = Loadings, -pattern1, -seed) %>%
  mutate(Chemicals = fct_inorder(Chemicals)) %>% 
  mutate(Group = ifelse(str_detect(Chemicals, "BDE"), "PBDEs",
                                                     ifelse(str_detect(Chemicals, "^M"), "Phthalates",
                                                            ifelse(str_detect(Chemicals, "pcb"), "PCBs",
                                                                   "Phenols")))) %>% 
  ggplot(aes(x = Chemicals, y = Loadings)) + geom_col(aes(fill = Group)) +
  facet_wrap(.~pattern1) + theme_bw() +
  theme(axis.text.x = element_blank(),
        legend.position = "bottom",
        strip.background = element_blank())

eh_over_reorder %>% 
  gather(key = Chemicals, value = Loadings, -pattern1, -seed) %>%
  mutate(Chemicals = fct_inorder(Chemicals)) %>% 
  mutate(Group = ifelse(str_detect(Chemicals, "BDE"), "PBDEs",
                                                     ifelse(str_detect(Chemicals, "^M"), "Phthalates",
                                                            ifelse(str_detect(Chemicals, "pcb"), "PCBs",
                                                                   "Phenols")))) %>% 
  ggplot(aes(x = Group, y = Loadings)) + geom_jitter(aes(color = Group), alpha = 0.5, size = 0.1, height = 0) +
  geom_boxplot(aes(x = Group, color = Group), width = .45, outlier.shape = NA) +
  facet_wrap(.~pattern1) + theme_bw() +
  theme(legend.position = "none",
        strip.background = element_blank())
```

## Standardize

### Loadings

```{r}
standardize <- function (any_matrix) {
  apply(any_matrix, 2, function(x) x/sd(x))
  }

dist_norm <- eh_dist_reorder %>% 
  select(-pattern1) %>% 
  nest(reorder_eh = pcb28:bpa) %>% 
  left_join(., full_dist, by = "seed") %>% 
  filter(!(seed %in% c(19, 21, 39))) %>% 
  mutate(sd_patterns = map(patterns, ~standardize(.x)),
         sd_loadings = map(reorder_eh, ~standardize(.x))) %>% 
  mutate(norm_loadings = map2(sd_patterns, sd_loadings, 
                              ~norm(as.matrix(.x) - as.matrix(.y), "F") / 
                                norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_loadings) %>% 
  pull(norm_loadings)

over_norm <- eh_over_reorder %>% 
  select(-pattern1) %>% 
  nest(reorder_eh = pcb28:bpa) %>% 
  left_join(., full_over, by = "seed") %>% 
  filter(!(seed %in% c(26, 64))) %>% # Two seeds with 5 patterns
  mutate(sd_patterns = map(patterns, ~standardize(.x)),
         sd_loadings = map(reorder_eh, ~standardize(.x))) %>% 
  mutate(norm_loadings = map2(sd_patterns, sd_loadings, 
                              ~norm(as.matrix(.x) - as.matrix(.y), "F") / 
                                norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_loadings) %>% 
  pull(norm_loadings)
  
cor_norm <- eh_cor_reorder %>% 
  select(-pattern1) %>% 
  nest(reorder_eh = pcb28:bpa) %>% 
  left_join(., full_cor, by = "seed") %>% 
  mutate(sd_patterns = map(patterns, ~standardize(.x)),
         sd_loadings = map(reorder_eh, ~standardize(.x))) %>% 
  mutate(norm_loadings = map2(sd_patterns, sd_loadings, 
                              ~norm(as.matrix(.x) - as.matrix(.y), "F") / 
                                norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_loadings) %>% 
  pull(norm_loadings)

summary(over_norm) 
summary(cor_norm) 
summary(dist_norm)

(sum(over_norm) + sum(cor_norm) + sum(dist_norm)) / 300
```

### Scores
```{r}
standardize <- function (any_matrix) {
  apply(any_matrix, 2, function(x) x/sd(x))
  }

dist_norm <- scores_ordered_dist %>% 
  select(seed, ewa_reordered, scores) %>% 
  filter(!(seed %in% c(19, 21, 39))) %>% 
  mutate(sd_scores = map(scores, ~standardize(.x)),
         sd_ewa = map(ewa_reordered, ~standardize(.x))) %>% 
  mutate(norm_scores = map2(sd_scores, sd_ewa, 
                              ~norm(as.matrix(.x) - as.matrix(.y), "F") / 
                                norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_scores) %>% 
  pull(norm_scores)

over_norm <- scores_ordered_over %>% 
  select(seed, ewa_reordered, scores) %>% 
  filter(!(seed %in% c(26, 64))) %>% 
  mutate(sd_scores = map(scores, ~standardize(.x)),
         sd_ewa = map(ewa_reordered, ~standardize(.x))) %>% 
  mutate(norm_scores = map2(sd_scores, sd_ewa, 
                              ~norm(as.matrix(.x) - as.matrix(.y), "F") / 
                                norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_scores) %>% 
  pull(norm_scores)
  
cor_norm <- scores_ordered_cor %>% 
  select(seed, ewa_reordered, scores) %>% 
  mutate(sd_scores = map(scores, ~standardize(.x)),
         sd_ewa = map(ewa_reordered, ~standardize(.x))) %>% 
  mutate(norm_scores = map2(sd_scores, sd_ewa, 
                              ~norm(as.matrix(.x) - as.matrix(.y), "F") / 
                                norm(as.matrix(.x), "F"))) %>% 
  unnest(norm_scores) %>% 
  pull(norm_scores)

summary(over_norm) 
summary(cor_norm) 
summary(dist_norm)

(sum(over_norm) + sum(cor_norm) + sum(dist_norm)) / 300
```